# Application name (optional but nice)
spring.application.name=firstapi

# Server runs on port 8080 (default, but you can change it)
server.port=8080

# Optional: Enable detailed Spring logs
logging.level.org.springframework=DEBUG
